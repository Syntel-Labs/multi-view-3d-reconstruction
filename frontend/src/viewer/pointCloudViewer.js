// Visor de nubes de puntos en Three.js.
// Pendiente: setup de Scene/Camera/Renderer, OrbitControls, PLYLoader y API de refresco.

/**
 * Inicializa el visor Three.js dentro del contenedor dado.
 * @param {HTMLElement} _root - Elemento del DOM que aloja el canvas.
 */
export function initViewer(_root) {
  // Pendiente: implementacion de la persona C.
}
